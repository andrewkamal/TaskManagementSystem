namespace TaskManagementSystem.Models
{
    public enum Dept
    {
        None,
        HR,
        IT,
        Payroll
    }

    public enum Status
    {
        Todo,
        InProgress,
        Completed
    }

    public enum Priority
    {
        Low,
        Medium,
        High,
        Urgent,
        Immediate
    }
}
